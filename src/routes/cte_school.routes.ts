import { Router } from "express"

const router = Router()

// // Get school districts for a specific CTE school with student counts
// router.get("/cte-school/:id/districts", getSchoolDistricts)

// // Get all school districts with student counts per CTE school
// router.get("/districts/with-counts", getAllSchoolDistrictsWithCounts)

export default router
